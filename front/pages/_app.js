import '@styles/globals.css'
import '@styles/app.sass'
import '@styles/Journal.sass';
import '@styles/Broadcaster.sass';

function MyApp({ Component, pageProps }) {
  return <Component {...pageProps} />
}

export default MyApp
